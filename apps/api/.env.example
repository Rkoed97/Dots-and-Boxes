# Environment for apps/api
# Aligns with docker-compose.yml (db service on localhost:5432 due to port mapping)
DATABASE_URL=postgresql://dots:dots_change_me@localhost:5432/dots
CORS_ORIGIN=http://localhost:3000
PORT=4000
